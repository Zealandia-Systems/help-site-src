name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: |
          npm install
      - run: |
          npm run build
      - name: Generate API module
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |

          git clone https://user:$GITHUB_TOKEN@github.com/Zealandia-Systems/help-site.git # This works

          ls

          cd help-site
          
          ls

          cp -r ../build ./

          git add ./
          git commit -m"pipeline push"

          git config --global user.email "lachlan.alsop@gmail.com"
          git config --global user.name "Lachlan Alsop Pipeline"

          git push
